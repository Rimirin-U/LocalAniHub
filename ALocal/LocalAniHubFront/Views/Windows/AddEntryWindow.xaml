<ui:FluentWindow x:Class="LocalAniHubFront.Views.Windows.AddEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LocalAniHubFront.Views.Windows"
        xmlns:components="clr-namespace:LocalAniHubFront.Views.Components"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Width="1100"
        Height="650"
        mc:Ignorable="d"
        ExtendsContentIntoTitleBar="True"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        AllowsTransparency="False"
        Title="AddEntryWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar x:Name="TitleBar"
                     Grid.Row="0"
                     Title="添加收藏"
                     CloseWindowByDoubleClickOnIcon="True"/>

        <Grid x:Name="MainGrid"
              Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="80"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Margin="8,0,8,0"
                           Text="拖动以添加"
                           Style="{StaticResource BodyTextBlockStyle}"
                           FontFamily="Microsoft YaHei"/>

                <ItemsControl x:Name="MetadataFromEntryInfoSetItems"
                              Grid.Row="1"
                          Margin="0,0,8,0"
                          ItemsSource="{Binding ViewModel.MetadataFromEntryInfoSet}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <components:KeyValueControl HorizontalAlignment="Left" Margin="8,8,0,0"
                                                    Pair="{Binding}" IsDragEnabled="True"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </Grid>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#10000000"/>

            <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Auto"
                          Grid.Column="0">
                <Grid x:Name="MainEditGrid">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" x:Name="TopRow"/>
                        <RowDefinition Height="Auto" x:Name="MainInfoRow"/>
                        <RowDefinition Height="Auto" x:Name="SettingRow"/>
                        <RowDefinition Height="*" x:Name="MetadataRow"/>
                    </Grid.RowDefinitions>

                    <Grid x:Name="TopGrid"
                          Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" x:Name="ImageArea"/>
                            <ColumnDefinition Width="2*" x:Name="TitleArea"/>
                        </Grid.ColumnDefinitions>

                        <ui:Image Source="{Binding ViewModel.KvImageSource}"/>

                    </Grid>



                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
</ui:FluentWindow>
