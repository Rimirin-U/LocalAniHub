<Window x:Class="LocalAniHub.Views.Windows.ResourceSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:LocalAniHub.ViewModels.Windows"
        mc:Ignorable="d"
        Title="ResourceSearchWindow" Height="400" Width="600">

    <Window.DataContext>
        <vm:ResourceSearchViewModel />
    </Window.DataContext>

    <StackPanel>
        <Button Content="Add TextBox" Command="{Binding AddTextBoxCommand}" Margin="10" />

        <ScrollViewer Height="300">
            <ItemsControl ItemsSource="{Binding TextBoxes}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBox Width="100" Margin="5"
                                 Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                                 KeyDown="TextBox_KeyDown" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </StackPanel>
</Window>